# Ariadna: Полный отчёт — от курирования до деплоя

**Дата:** 11–13 июля 2026  
**Автор:** Roman Nekrasov  
**Модель:** Qwen3.5-4B-Base → CPT → SFT → GRPO → GGUF

---

## Содержание

1. [Фаза 0–6: Курирование данных](#фаза-06-курирование-данных)
2. [Фаза 7: CPT (Continued Pretraining)](#фаза-7-cpt)
3. [Фаза 8: SFT (LoRA Fine-Tuning)](#фаза-8-sft)
4. [Фаза 9: RL (GRPO)](#фаза-9-rl)
5. [Деплой: GGUF + локальный сервер](#деплой-gguf)
6. [Итоговые метрики](#итоговые-метрики)
7. [Стоимость](#стоимость)
8. [Известные ограничения](#известные-ограничения)

---

## Фаза 0–6: Курирование данных

### Инвентаризация
- **Статей:** 12,729 PDF (arXiv, ML/AI, 2020–2026)
- **Дедупликация:** SHA-256 по полному тексту
- **Манифест:** `manifest.jsonl` — 12,729 записей с приоритетами P0/P1/P2/skip

### Извлечение концептов
- **Пайплайн:** `_forever_extract.py` → `_parallel_extract.py` (30 воркеров) → DeepSeek API
- **Извлечено:** 10,892 статей (85.6%)
- **Концептов:** 124,547 (в базе)
- **Скорость:** ~700 статей/час на пике

### Таксономия
- **14 типов** (v4.0): architectural_component, algorithmic_primitive, procedural_step, diagnostic_metric, data_structure, boundary_condition, behavioral_archetype, behavioral_capability, behavioral_pattern, benchmark, empirical_finding, hypothesis, experiment_configuration, interpretive_framework
- **Уровни:** α (формальный), β (описательный), γ (сырой — отбрасывается гейтом)

### Валидация
- **Гейт v4.0:** type ∈ таксономия, level ∈ {α,β}, body ≥ 200 символов, type-консистентность (frontmatter vs body), slug-формат, проверка на конфабуляции VRAM
- **Проблемы исправлены:** 8 shadow-директорий мигрировано, 19 α-концептов без formality, 7 case-коллизий, 292 битых строки в манифесте

### Производные артефакты
| Артефакт | Размер | Описание |
|----------|--------|----------|
| CPT-корпус | 65.8M токенов | 151K документов (cards + papers + pedagogy + replay) |
| ML-экспорт | 626K примеров | QA-пары из FAQ (не шаблоны!), определения, формулы, отношения, contrastive |
| classify.jsonl | 38,019 задач | RL-задачи (очищены от γ) |
| Педагогика | 8,660 мини-глоссариев, 4,580 треков, 6,311 дайджестов | Топсорт графа концептов |

---

## Фаза 7: CPT

### Конфигурация
- **Базовая модель:** Qwen/Qwen3.5-4B-Base
- **Корпус:** 65.8M токенов (151K документов, пакинг с EOS-разделителями)
- **Архитектура:** 32 слоя (24 Gated DeltaNet + 8 full attention), 2560 hidden, ~4.2B параметров
- **Гиперпараметры:**
  - seq_len=2048, micro_batch_size=1, grad_accum=8
  - lr=1e-5, AdamW 8-bit, bf16
  - 1 эпоха, ~37,643 батчей

### Результаты
- **Начальный loss:** 6.75
- **Конечный loss:** 0.85
- **Пропускная способность:** 5,000 tok/s (с быстрым путём causal_conv1d + fla)
- **Пиковый VRAM:** 32.9 GB
- **Время:** ~4–5 часов (эффективное, без учёта краша диска)
- **GPU:** 1× H100 PCIe 80GB, Vast AI

### Инцидент: краш диска
- **Причина:** чекпойнты каждые 500 шагов (18 × ~8GB = 134GB на диске 150GB)
- **Решение:** watchdog-скрипт + ротация (keep-last-2), интервал чекпойнтов 500 → 2000
- **Потеряно:** ~6 часов прогресса (перезапуск с нуля)

### Артефакты
- Safetensors: `Rob1234567/qwen3.5-4b-ariadna-cpt-v1` (~8 GB)
- GGUF: `model-Q4_K_M.gguf` (~2.5 GB)

---

## Фаза 8: SFT

### Конфигурация
- **Метод:** LoRA (rank=64, alpha=128, dropout=0.05)
- **Данные:** 10,000 QA-пар из `ml_export/definitions.jsonl`
- **Формат:** Qwen chat_template (`<|im_start|>user/assistant`)
- **Гиперпараметры:**
  - batch_size=1, grad_accum=32, gradient_checkpointing
  - lr=1e-4, 2 эпохи, 626 шагов
  - eager attention (causal_conv1d отсутствовал на образе hiyouga/verl)

### Результаты
- **Начальный loss:** 0.82
- **Конечный loss:** 0.72 (плато — адаптер мал для снижения)
- **Trainable params:** 129.9M (3.0% от всех)
- **Скорость:** 10.2 сек/шаг (eager fallback)
- **Время:** ~2 часа
- **GPU:** тот же H100

### Исправление
- `merge_and_unload()` для сохранения полных весов (PeftModel → полная модель)

### Артефакт
- Safetensors: `Rob1234567/qwen3.5-4b-ariadna-sft-v1` (8.4 GB, полные веса)

---

## Фаза 9: RL (GRPO)

### Конфигурация
- **Метод:** PPO-clip с KL-штрафом (ручная реализация, без TRL/vLLM)
- **Reference модель:** Qwen3.5-4B-Base (CPT)
- **Policy модель:** SFT-модель (merge_and_unload)
- **Данные:** 200 classify-промптов из classify_sample.jsonl
- **Гиперпараметры:**
  - K=4 (генераций на промпт), lr=1e-6, β=0.01 (KL)
  - 100 шагов, max_new_tokens=128, temperature=0.9

### Результаты
- **Reward:** 0.10 (плато — reward-функция «contains gold_type» слишком простая)
- **Loss:** ~1.0
- **Время:** ~15 минут
- **GPU:** 1× H100 PCIe 80GB (CUDA 13.0)

### Стек (собран за 1 итерацию по навыку qwen35-rl-vast-env)
- vLLM 0.20.2 + torch 2.11.0+cu130
- fla 0.5.1 + causal_conv1d 1.6.2 (сборка из исходников)
- transformers 5.9.0 (для model_type qwen3_5)
- nvcc 13.0 (выровнен с torch.version.cuda)

### Артефакт
- Safetensors: `Rob1234567/qwen3.5-4b-ariadna-grpo-v1` (17.9 GB, публичный)

---

## Деплой: GGUF

### Конвертация
- **Инструмент:** llama.cpp (mainline, сборка из исходников)
- **Формат:** bf16 → IQ4_K_XS (оптимизирован под RTX 4080 16GB)
- **Платформа:** Vast AI (RTX 3090, $0.18/ч — GGUF не требует GPU, CPU-only конвертация)
- **Размер:** ~2.5 GB (ожидаемый)

### Локальный запуск (RTX 4080 16GB)
```bash
huggingface-cli download Rob1234567/qwen3.5-4b-ariadna-grpo-v1 model-IQ4_K_XS.gguf --local-dir ~/models/
cd ~/llama.cpp && cmake -B build -DGGML_CUDA=ON && cmake --build build -j $(nproc)
./build/bin/llama-server -m ~/models/model-IQ4_K_XS.gguf -ngl 99 -fa -c 32768 -ctk q8_0 -ctv q8_0 --host 127.0.0.1 --port 8081
```

Проверка: `curl http://127.0.0.1:8081/v1/chat/completions -d '{"messages":[{"role":"user","content":"Что такое Gated DeltaNet?"}]}'`

---

## Итоговые метрики

| Показатель | Значение |
|------------|----------|
| Статей обработано | 10,892 / 12,729 (85.6%) |
| Концептов в базе | 124,547 |
| Типов концептов | 14 |
| CPT-токенов | 65.8M |
| CPT loss | 6.75 → 0.85 |
| SFT loss | 0.82 → 0.72 |
| GRPO reward | 0.10 (пилот) |
| Общее время обучения | ~9 часов |
| Версий стека RL перебрано | 19 (успешная — 1) |

---

## Стоимость

| Статья | Сумма |
|--------|-------|
| CPT (успешный прогон) | ~$10 |
| CPT (краш диска, потеряно) | ~$10 |
| SFT | ~$4 |
| RL | ~$3 |
| Отладка/перезапуски (19 итераций стека) | ~$25 |
| GGUF конвертация | ~$1 |
| **Итого** | **~$53** |

---

## Стратегическая цель

Этот эксперимент — часть архитектуры **агентного хозяйства** (Agent Estate). Большим моделям (GPT-4, Claude, Gemini) в качестве специализированных субагентов-экспертов понадобятся доменные модели с одним тулом: глубоким знанием ML/AI-концептов. Ariadna — первая такая модель.

**План развёртывания:**
1. Доменная модель (Qwen3.5-4B-Ariadna) → GGUF → llama.cpp
2. Субагент-эксперт с одним тулом: RAG по 124K концептов
3. Главный агент (boundary) вызывает субагента для ML/AI-вопросов
4. Развёртывание на NVIDIA GB10 (чип следующего поколения с unified memory)

GB10 позволит держать модель в VRAM постоянно, обеспечивая задержку <100ms на инференс. Это критично для агентных сценариев, где субагент должен отвечать за один ход диалога.

## Известные ограничения

1. **RL reward 0.10** — reward-функция «contains gold_type» слишком простая. Нужна семантическая оценка (LLM-judge на 50+ calibrated example'ах)
2. **CPT-корпус мал** — 65.8M токенов на 4.2B-модель = ~16 токенов/параметр. Рекомендуется ≥ 200M для следующей итерации
3. **SFT на базовой модели** — без instruction-tuning модель ожидает definition-style промпты, а не диалог
4. **GGUF IQ4_K_XS** — минимальное качество (4-bit). IQ4_K_M или IQ3_M лучше для инференса
5. **Пакинг без cross-document attention mask** — документы разделены EOS, но attention не изолирован (допустимо для CPT)
6. **Split не paper-level** — 5% eval содержит концепты из тех же статей, что и train

---

## Воспроизведение

Все скрипты и данные открыты:

- **Код:** https://github.com/romannekrasovaillm/ariadna-training
- **Корпус:** https://huggingface.co/datasets/Rob1234567/ariadna-cpt-corpus
- **Модель:** https://huggingface.co/Rob1234567/qwen3.5-4b-ariadna-grpo-v1

Требования: GPU ≥ 80GB (H100/A100), 150GB+ диск, Python 3.11+, CUDA 13.0 (для RL).
